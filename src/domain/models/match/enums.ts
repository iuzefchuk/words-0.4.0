export enum Difficulty {
  High = 'High',
  Low = 'Low',
  Medium = 'Medium',
}

export enum MatchResult {
  Lose = 'Lose',
  Tie = 'Tie',
  Undecided = 'Undecided',
  Win = 'Win',
}

export enum MatchType {
  Classic = 'Classic',
  Random = 'Random',
}
